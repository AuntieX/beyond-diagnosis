---
import Layout from "../../layouts/Layout.astro";
import EntryCard from "../../components/EntryCard.astro";
import Reveal from "../../components/Reveal.astro";
import { getLifeStageEntries, distinctCancerTags, cancerTagsList } from "../../lib/content";
import { buildConfig, fadeUpGroup, fadeUpReveal, fadeInReveal } from "../../lib/motion";
import { PATHS } from "../../lib/paths";

const meta = PATHS.find((p) => p.key === "risk")!;

const allEntries = await getLifeStageEntries("Environment");
const cancerTypes = distinctCancerTags(allEntries);
const activeType = Astro.url.searchParams.get("type") || "";
const entries = activeType ? allEntries.filter((e) => cancerTagsList(e.cancer_tags).includes(activeType)) : allEntries;

const keys = entries.map((_, i) => `risk-entry-${i}`);
const motion = buildConfig([
  fadeUpReveal("risk-heading", 0),
  fadeUpReveal("risk-subhead", 100),
  fadeInReveal("risk-pills", 200),
  ...fadeUpGroup(keys),
]);

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  name: "Ground Rules — Beyond Diagnosis",
  url: new URL("/paths/risk", Astro.site ?? Astro.url.origin),
};
---

<Layout
  title="I'm reducing my risk"
  description="Radon, water, air, and the everyday exposures worth knowing about — practical, not paranoid."
  motion={motion}
  jsonLd={jsonLd}
  canonicalPath="/paths/risk"
>
  <section class="bg-white py-14 sm:py-20">
    <div class="mx-auto max-w-6xl px-5 sm:px-8">
      <interact-element data-interact-key="risk-heading" class="block">
        <div>
          <p class="font-data text-xs uppercase tracking-wide text-sage-dark">{meta.subbrand}</p>
          <h1 class="mt-2 font-display text-3xl font-semibold text-navy sm:text-4xl">{meta.tagline}</h1>
        </div>
      </interact-element>
      <interact-element data-interact-key="risk-subhead" class="block">
        <p class="mt-3 max-w-2xl text-navy/70">
          Radon, water, air, and the everyday exposures worth knowing about — practical, not paranoid.
        </p>
      </interact-element>

      {
        cancerTypes.length > 0 && (
          <interact-element data-interact-key="risk-pills" class="block">
            <div class="mt-8 flex flex-wrap gap-2" role="group" aria-label="Filter by cancer type">
              <a
                href="/paths/risk"
                class:list={[
                  "rounded-full px-4 py-2 text-sm font-semibold transition-colors",
                  !activeType ? "bg-navy text-cream" : "bg-rose text-navy hover:bg-rose/70",
                ]}
              >
                All
              </a>
              {cancerTypes.map((type) => (
                <a
                  href={`/paths/risk?type=${encodeURIComponent(type)}`}
                  class:list={[
                    "rounded-full px-4 py-2 text-sm font-semibold transition-colors",
                    activeType === type ? "bg-navy text-cream" : "bg-rose text-navy hover:bg-rose/70",
                  ]}
                >
                  {type}
                </a>
              ))}
            </div>
          </interact-element>
        )
      }

      <h2 class="sr-only">Risk-reduction guides{activeType ? ` — ${activeType}` : ""}</h2>
      {
        entries.length === 0 ? (
          <p class="mt-10 text-navy/70">No guides match that filter yet — check back soon as we expand this section.</p>
        ) : (
          <div class="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
            {entries.map((entry, i) =>
              i === 0 ? (
                <EntryCard entry={entry} basePath="/environment" tone="sage" eager />
              ) : (
                <Reveal interactKey={keys[i]}>
                  <EntryCard entry={entry} basePath="/environment" tone="sage" />
                </Reveal>
              )
            )}
          </div>
        )
      }
    </div>
  </section>
</Layout>
